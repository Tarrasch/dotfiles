" " I disabled everything since I think this what causes my vim to crash every
" now and then.
set nocompatible
source /usr/share/vim/google/google.vim
filetype plugin indent on

Glug add_usings plugin[mappings]
